#include <iostream>
#include <vector>
using namespace std;
class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        
    }
};

void main()
{
    vector<int>obj(6);
    int const num[6] = { 1, 3, 5, 6 };
    for (int i = 0; i <= 3; i++)
    {
        obj[i] = num[i];
    }
}